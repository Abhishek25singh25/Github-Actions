name: Day 5 Workflow

on:
  push:
    branches:
      - main

jobs:
  env-practice:
    runs-on: ubuntu-latest
    env:
      APP_NAME: MyApp
    steps:
      - name: Print Application Name
        run: echo "Application Name $APP_NAME"
