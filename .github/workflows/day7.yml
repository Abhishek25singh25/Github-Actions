name: Day-7-yml
on:
  workflow_dispatch:


jobs:
  build:
    runs-on: ubuntu-latest
    steps:

      - name: checkout repository
        uses: actions/checkout@v4

      - name: Build the pipeline
        run: echo "Buildig project"

  Deploy:
    if: github.ref_name == 'main'
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploying to production
        run: echo "Deploying project"
      

